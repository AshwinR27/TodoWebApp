{
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "install": "concurrently \"npm install --prefix client\" \"npm install --prefix server\"",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
